apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: cloudflare
spec:
  acme:
    solvers:
    - dns01:
      cloudflare:
        apiTokenSecretRef:
        name: cloudflare-secrets
        key: cloudflare_api_token_secret