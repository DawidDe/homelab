---
services:
  ddns:
    image: favonica/cloudflare-ddns:latest
    container_name: ddns
    read_only: true
    user: "1001:1004"
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    env_file:
      - .env
    restart: unless-stopped